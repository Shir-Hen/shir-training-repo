pipeline {
    agent any
    stages {
	
        stage ('create image') {
            steps {
                bat 'docker build . -t my-second-image -f docker/Dockerfile'
                echo 'Shir finished Building her Project'
            }
		}  
        
    }
}
