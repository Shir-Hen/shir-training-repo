pipeline {
    agent any
    tools { 
        maven 'maven-3.6.3'
    }
    stages {
	
        stage ('create image') {
            steps {
                bat 'docker build -t my-first-image docker/Dockerfile'
                echo 'Shir finished Building her Project'
            }
		}  
        
    }
}
